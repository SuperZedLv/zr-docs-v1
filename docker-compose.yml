services:
  zr-docs-v1:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: zr-docs-v1
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      NODE_ENV: production
    restart: unless-stopped
